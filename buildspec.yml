version: 0.2

phases:
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - aws s3 cp --recursive ./build s3://www.giveback/
      - aws cloudfront create-invalidation --distribution-id E1A2B3C4D5E6F7 --paths /\*